class Endpoints {
  static String baseUrl = 'https://dummyjson.com';
  static Duration connectionTimeout = Duration(milliseconds: 30000);
  static Duration receiveTimeout = Duration(milliseconds: 30000);

  static String login = '/auth/login';
}
